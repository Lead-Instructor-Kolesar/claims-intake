"""Pipeline runner. Day 5 work.

Accepts a build identifier, a question set, and a run identifier, and executes
ingestion checks, retrieval, reranking, assembly, answering, and scoring, writing
every record stream and emitting the report. With no stage selection it runs
everything.

One invocation must reproduce every figure in reports/rag-eval.md.
"""

from __future__ import annotations


def main() -> int:
    """Day 5 assignment, instruction 6."""
    raise NotImplementedError


if __name__ == "__main__":  # pragma: no cover
    raise SystemExit(main())
